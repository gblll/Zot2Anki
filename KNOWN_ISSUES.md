# 已知问题与开发状态

当前仓库保存的是开发中的源码，不是已完成安全修复的稳定发行版。
以下问题来自 2026-09-04 的代码审查。个人配置现已从源码分离；下列核心同步问题仍未修复。

项目的核心目标是：将已整理在 Zotero Note 中的生词更新到 Anki，在原卡上修改内容，并保留复习记录及个人 `Notes`。

## 优先修复：词条身份与失败保护

### 1. 合并词条拆分后可能覆盖同一张卡

一条 Anki 笔记拥有多个 annotation key 时，如果这些来源后来变成不同单词，当前同步循环可能把多个输入依次写入同一条笔记。临时数据库测试中，报告显示更新两条，实际只保留后写入的单词。

- 位置：[sync_cards](scripts/sync_vocabulary.py)
- 需要：在写入前生成完整匹配计划，检查同一笔记是否被多条输入占用，并明确词条拆分、合并的处理规则；无法确定时应停止，不猜测覆盖。

### 2. 同步中途失败不会自动回滚

前面的笔记可能已经更新，后面的词条才触发冲突。重新打开临时数据库后，之前的修改仍然存在。因此“同步失败”不代表 collection 完全未变。

- 位置：[同步主流程](scripts/sync_vocabulary.py)
- 需要：完整冲突预检、单次运行锁、清晰的失败状态和经过验证的恢复流程。
- 当前建议：先在测试配置或数据库副本验证；保留同步前备份，不把现有版本用于无人值守同步。

## 其他已确认的问题

### 3. `clean.apkg` 尚不能保证只包含目标笔记

生成过程只清理带 `Zotero2Anki` 标签的目标类型笔记，但按整个牌组导出。如果该牌组混入其他私人卡片，它们也可能被打包，当前校验器又只统计目标笔记类型，可能漏检。

- 位置：[干净包生成](scripts/sync_vocabulary.py)、[APKG 校验](tests/validate_apkg.py)
- 需要：按允许名单构建分享包，并校验包内全部笔记、卡片和媒体，而非只统计目标类型。
- 当前建议：**不要把当前生成的 `clean.apkg` 当成已经完成隐私清理的分享包。**

### 4. 已处理：入口配置统一与个人路径分离

PowerShell 与 Python 入口现在共用 `scripts/local_config.py`，读取不上传的 `config.local.json`。源码不再固定开发电脑的路径、账户或笔记名称；未指定 Anki 配置时停止并提示，不再自动选择某个账户。

- 本地配置优先于通用环境默认值，命令行参数优先于本地配置。
- README 已拆为英文主页和中文说明；联网例句默认关闭。
- 注意：这些修改不解决词条匹配冲突、部分写入或分享包隐私边界问题。

### 5. 例句匹配可能命中其他单词内部

当前匹配会移除空格、标点后进行子串搜索，例如 `art` 可能命中 `article`，`can` 可能命中 `cancer`。

- 位置：[find_term_span](scripts/vocabulary_examples.py)
- 需要：保留词边界，同时支持 PDF 跨行连字符的匹配。

### 6. 网络故障产生的空结果不会自动刷新

请求失败后可能缓存空结果。网络恢复后，后续同步仍直接使用该空缓存，不再次查询。

- 位置：[AcademicExampleClient.find](scripts/vocabulary_examples.py)
- 需要：区分“查询成功但无结果”和“请求失败”，增加过期时间及手动刷新机制。

### 7. 直接同步与 TSV 的 Word 编码不一致

TSV 会转义单词中的 HTML 特殊字符，直接同步却原样写入 Anki 的 HTML 字段。相同的字面文本可能在两个入口中显示不同。

- 位置：[直接写入](scripts/sync_vocabulary.py)、[TSV 序列化](scripts/export_vocabulary_note.py)
- 需要：统一字段编码和已有词条匹配方式。

## 验证范围

- Python 单元测试覆盖解析、模板、启动器、共享配置与基本仓库隐私检查；JavaScript 测试覆盖旧插件纯函数。运行命令见 README，测试数量随开发变化。
- Anki 26.5 临时 collection 测试覆盖普通新增、按 key 更新、Notes/GUID 保留、缺失标记和 APKG 回读。
- 上述拆分覆盖、部分写入、分享包边界、子串匹配和故障缓存问题由额外的临时测试复现，尚未加入仓库回归测试。
- 通过现有测试不代表所有同步边界已安全。
- 真实 PDF 提取、移动端卡片显示及旧 Zotero 插件生命周期仍需要补充端到端验证。

## 后续整理范围

先修复核心同步并补回归测试，再将联网例句、APKG 分享、旧 Zotero 插件与日常同步分开。旧插件当前保留作历史源码，并不是推荐入口。

公开发行前还需明确许可证、核对独立第三方组件的授权，并完善安装配置说明及自动化测试。自制卡片模板不再列为待核对的第三方模板来源。
